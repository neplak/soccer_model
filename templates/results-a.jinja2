{% extends "base.jinja2" %}
{% block content %}
    
<div class="accordion" id="accordionExample">
{%if gameschedule.toursPlayed %}
    {% for tour in gameschedule.toursPlayed %}
        <div class="accordion-item">
            <h2 class="accordion-header" id="heading{{loop.index}}">
                <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapse{{loop.index}}" aria-expanded="false" aria-controls="collapse{{loop.index}}">
                    Match Day {{loop.index}}
                </button>
            </h2>
        
        <div id="collapse{{loop.index}}" class="accordion-collapse collapse" aria-labelledby="heading{{loop.index}}" data-bs-parent="#accordionExample">
            <ol class="list-group">
            {% for game in tour %}
                <li class="list-group-item">
                    {{game.home.name}}({{game.home.location}})-{{game.away.name}}({{game.away.location}})
                    <br>
                    {{game.score[0]}}:{{game.score[1]}}({{game.score1[0]}}:{{game.score1[1]}})
                </li>
            {% endfor %}
            </ol>
        </div>
        </div>
        

    {% endfor %}
    </div>
    {% endif %}
    </div>

    {# a comment #}
{% endblock %}